# Большой тест по Pandas — 70 вопросов

## Исходные данные

Для вопросов 1–5, 6–7 и некоторых последующих используй:

```python
import pandas as pd
import numpy as np

df = pd.DataFrame({
    "name": ["A", "B", "C", "D"],
    "age": [20, 30, 40, 25],
    "salary": [300, 200, 100, 400],
    "city": ["Moscow", "Moscow", "Kazan", "Kazan"]
})
```

---

# Блок 1. Основы DataFrame и Series

### 1. Что вернёт?

```python
df["age"]
```

- `DataFrame`
- `Series`
- `list`
- `numpy.ndarray`

---

### 2. Что вернёт?

```python
df[["age"]]
```

- `DataFrame`
- `Series`
- `list`
- `numpy.ndarray`

---

### 3. Что вернёт?

```python
df.iloc[2, 1]
```

---

### 4. Что вернёт?

```python
df.loc[2, "salary"]
```

---

### 5. В чём принципиальная разница между `loc` и `iloc`?

Объясни своими словами.

---

# Блок 2. Фильтрация

### 6. Что получится в результате?

```python
df[df["age"] > 25]
```

Запиши результат.

---

### 7. Что делает этот код?

```python
df.loc[df["age"] > 25, "name"]
```

Какой будет результат?

---

### 8. Почему такой код не работает?

```python
df[(df["age"] > 20) and (df["salary"] > 100)]
```

Что нужно использовать вместо `and`?

---

### 9. Напиши код, который оставит строки, где одновременно:

- `age > 25`
- `salary < 300`

---

### 10. Напиши код, который:

- оставит только людей с `age > 25`;
- из них выберет только столбцы `name` и `salary`.

---

# Блок 3. `loc` и `iloc`

### 11. Какие строки выберет?

```python
df.iloc[1:4]
```

---

### 12. Какие строки выберет?

```python
df.loc[1:4]
```

---

### 13. Почему результаты вопросов 11 и 12 отличаются?

---

### 14. Что делает?

```python
df.loc[:, "salary"]
```

Какой объект вернётся?

Сравни с:

```python
df["salary"]
```

---

### 15. Что делает?

```python
df.iloc[:, 1]
```

Какой столбец будет выбран?

---

# Блок 4. Сортировка

### 16. Отсортируй `df` по `salary` по возрастанию.

Какой получится порядок имён?

---

### 17. Отсортируй `df` по `salary` по убыванию.

Какой получится порядок имён?

---

### 18. Изменится ли исходный `df` после?

```python
df.sort_values("salary")
```

Если нет, как сохранить результат?

---

### 19. В чём разница между:

```python
df.sort_values("salary")
```

и

```python
df["salary"].sort_values()
```

---

# Блок 5. `value_counts()`

### 20. Что вернёт?

```python
df["city"].value_counts()
```

Запиши результат.

---

### 21. Как получить город, который встречается чаще всего?

---

### 22. Как получить максимальное количество повторений какого-либо города?

---

# Блок 6. `groupby`

Используй:

```python
df = pd.DataFrame({
    "user": ["A", "A", "B", "B", "B"],
    "price": [100, 300, 200, 500, 100]
})
```

### 23. Что делает?

```python
df.groupby("user")
```

Объясни своими словами.

---

### 24. Что получится?

```python
df.groupby("user")["price"].mean()
```

---

### 25. Что получится?

```python
df.groupby("user")["price"].sum()
```

---

### 26. Что получится?

```python
df.groupby("user")["price"].max()
```

---

### 27. Как одновременно получить для каждого пользователя:

- `mean`
- `sum`
- `min`
- `max`

---

### 28. Что считает?

```python
df.groupby("user")["price"].count()
```

Особенно важно: как `count()` работает с `NaN`?

---

# Блок 7. Пропущенные значения `NaN`

Используй:

```python
df = pd.DataFrame({
    "age": [20, 30, np.nan, 40],
    "salary": [100, np.nan, 300, 400]
})
```

### 29. Что вернёт?

```python
df.isna().sum()
```

---

### 30. Что вернёт?

```python
df["age"].mean()
```

---

### 31. Почему среднее возраста не является `NaN`, несмотря на наличие `NaN` в столбце?

---

### 32. Что делает?

```python
df["age"].fillna(0)
```

---

### 33. Изменится ли исходный столбец `age` после?

```python
df["age"].fillna(0)
```

Почему?

---

### 34. Напиши правильный код, чтобы заменить `NaN` в `age` на `0` и сохранить изменение в `df`.

---

### 35. Что делает?

```python
df.dropna()
```

Какие строки удаляются по умолчанию?

---

### 36. Для этого DataFrame:

```python
df = pd.DataFrame({
    "age": [20, 30, np.nan, 40],
    "salary": [100, np.nan, 300, 400]
})
```

Что останется после:

```python
df.dropna()
```

---

# Блок 8. `merge`

Используй:

```python
A = pd.DataFrame({
    "id": [1, 2, 3],
    "name": ["A", "B", "C"]
})

B = pd.DataFrame({
    "id": [1, 2, 4],
    "price": [100, 200, 300]
})
```

### 37. Что получится?

```python
A.merge(B, on="id", how="inner")
```

Какие `id` останутся?

---

### 38. Какие `id` останутся после?

```python
A.merge(B, on="id", how="left")
```

---

### 39. Какие `id` останутся после?

```python
A.merge(B, on="id", how="outer")
```

---

### 40. Что будет в `price` для `id=3` после:

```python
A.merge(B, on="id", how="left")
```

---

### 41. Что означает `how="inner"`?

---

### 42. Что означает `how="left"`?

---

# Блок 9. Many-to-many merge

### 43. Пусть слева есть:

```text
id
1
1
```

а справа:

```text
id
1
1
```

Сколько строк получится после:

```python
left.merge(right, on="id")
```

---

### 44. Почему получится именно столько строк?

Объясни механизм.

---

### 45. Если слева ключ `id=10` встречается 3 раза, а справа он встречается 4 раза, сколько строк с `id=10` получится после `merge`?

---

### 46. В каком случае возникает many-to-many merge?

---

# Блок 10. `concat` и `merge`

### 47. В чём принципиальная разница между `concat` и `merge`?

---

### 48. У тебя есть два DataFrame с одинаковыми столбцами:

```text
name age
A    20
B    30
```

и

```text
name age
C    25
D    40
```

Какой инструмент использовать, чтобы получить одну таблицу из четырёх строк?

---

### 49. У тебя есть:

```text
users:
user_id name
1       A
2       B
```

и:

```text
orders:
user_id price
1       100
2       300
```

Ты хочешь добавить информацию о заказах к пользователям по `user_id`.

Что использовать: `concat` или `merge`?

---

# Блок 11. Data Leakage

### 50. Почему это может быть leakage?

```python
mean = pd.concat([train, test])["age"].mean()

train["age"] = train["age"].fillna(mean)
test["age"] = test["age"].fillna(mean)
```

---

### 51. Как правильно вычислять среднее для заполнения пропусков в train/test?

Напиши код.

---

### 52. Какие из вариантов содержат leakage?

**A**

```python
mean = train["age"].mean()
train["age"] = train["age"].fillna(mean)
```

**B**

```python
mean = test["age"].mean()
train["age"] = train["age"].fillna(mean)
```

**C**

```python
mean = train["age"].mean()

train["age"] = train["age"].fillna(mean)
test["age"] = test["age"].fillna(mean)
```

**D**

```python
mean = train["age"].mean()
test["age"] = test["age"].fillna(mean)
```

---

# Блок 12. Смешанные вопросы

### 53. Для исходного DataFrame:

```python
df = pd.DataFrame({
    "name": ["A", "B", "C", "D"],
    "age": [20, 30, 40, 25],
    "salary": [300, 200, 100, 400]
})
```

Напиши код, который:

1. оставит людей с `salary > 200`;
2. оставит только `name` и `age`.

Какой будет результат?

---

### 54. Что произойдёт?

```python
df.groupby("user")["price"].mean()
```

если в `price` есть `NaN`?

Учитывается ли `NaN` при вычислении среднего?

---

### 55. Если:

```python
df["city"].value_counts()
```

вернул:

```text
Moscow    3
Kazan     2
Berlin    1
```

как получить число `2` для `Kazan`?

---

### 56. Напиши код, который:

1. оставит строки, где `age >= 18`;
2. из них возьмёт `salary`;
3. посчитает среднюю зарплату.

---

### 57. Перед выполнением большого `merge` у тебя есть:

```python
users
orders
```

Что нужно проверить, чтобы понять, не возникнет ли неожиданное увеличение количества строк?

---

### 58. Почему many-to-many merge может неожиданно «раздуть» DataFrame?

---

### 59. Как получить для каждого `user_id` количество **непропущенных** значений `price`?

---

### 60. У тебя есть таблица:

```text
user_id    date        price
1          2026-01-01  100
1          2026-01-05  200
2          2026-01-02  300
2          2026-01-10  400
```

Нужно оставить **самый свежий заказ каждого пользователя**.

Какая идея решения?

---

# Блок 13. Bonus — вопросы на внимательность

### 61. Что вернёт?

```python
df = pd.DataFrame({
    "user": ["A", "A", "A"],
    "price": [100, np.nan, 300]
})

df["price"].count()
```

---

### 62. Что вернёт?

```python
df = pd.DataFrame({
    "age": [20, 30, np.nan, 40]
})

df["age"].mean()
```

---

### 63. Что принципиально различается между:

```python
df[df["age"] > 30]
```

и:

```python
df.loc[df["age"] > 30, "age"]
```

---

### 64. Какой тип вернёт каждый вариант?

```python
df["age"]
```

и:

```python
df[["age"]]
```

---

### 65. Верно или неверно?

> `inner merge` гарантирует, что все строки из левой таблицы останутся в результате.

---

### 66. Верно или неверно?

> `left merge` сохраняет строки левой таблицы, но количество строк результата может увеличиться.

---

### 67. Верно или неверно?

> Если среднее для заполнения пропусков вычислить на `train + test`, это может привести к data leakage.

Объясни почему.

---

### 68. Верно или неверно?

> `groupby` всегда уменьшает количество строк DataFrame.

Объясни.

---

### 69. Верно или неверно?

> `merge` всегда уменьшает количество строк.

Объясни и приведи возможные варианты.

---

### 70. Верно или неверно?

> `value_counts()` можно использовать для подсчёта частоты классов целевой переменной.

Например:

```python
df["target"].value_counts()
```

---

# Дополнительные вопросы для самопроверки

После основных 70 вопросов попробуй без подсказки ответить ещё на следующие вопросы.

## A. Series и DataFrame

Почему:

```python
df["age"]
```

и:

```python
df[["age"]]
```

имеют разный тип?

---

## B. Фильтрация

Что произойдёт здесь?

```python
df[df["age"] > 20]["salary"]
```

А что здесь?

```python
df.loc[df["age"] > 20, "salary"]
```

---

## C. `loc`

Что означает каждый компонент?

```python
df.loc[condition, ["name", "salary"]]
```

---

## D. `merge`

Объясни разницу между:

```python
how="inner"
how="left"
how="right"
how="outer"
```

---

## E. Cardinality

Что означает:

```text
1:1
1:N
N:1
N:M
```

при `merge`?

---

## F. Leakage

Объясни своими словами:

> Почему можно использовать `train_mean` для заполнения `test`, но нельзя использовать `test_mean` для обучения?

---

## G. NaN

Почему:

```python
df["age"].mean()
```

может вернуть число, даже если в `age` есть `NaN`?

---

## H. `count`

Почему:

```python
df["age"].count()
```

и:

```python
len(df)
```

могут вернуть разные значения?

---

## I. `groupby`

Чем отличаются:

```python
df.groupby("user")
```

и:

```python
df.groupby("user")["price"].mean()
```

---

## J. Сортировка

Почему для задачи «найти последний заказ каждого пользователя» одной сортировки недостаточно?

Что нужно сделать после сортировки?

# Мои ответы на тест по Pandas

1. **b**

2. **b**

3. **200**

4. **300**

5. loc обращается по `[номер строки, название столбца]`, а iloc по индексам строки и столбца. В твоем примере результат будет один и тот же = 30

6. будет

```text
name age salary
B    30    200
C    40    300
```

то есть те строки, где `age > 25`

7. найдет все имена, у владельцев которых возраст > 25. Ответ

```text
index name
1    B
2    C
```

8. `and` нельзя писать в pandas нужно использовать `&`. А еще условия берутся в круглые скобки. Надо

```python
df[(df["age"] > 20) & (df["salary"] > 100)]
```

9.

```python
df[(df["age"] > 25) & (df["dalary"] < 300)]
```

10.

```python
df.loc[df["age"] > 25, ["name", "salary"]]
```

11. **1, 2, 3**

12. **1, 2, 3, 4**

13. потому что iloc берет справа невключительно, то есть как обычные питон индексы, а loc берет включительно

14. взять весь столбец salary. Насколько я понимаю, это то же самое что и `df["salary"]`?

15. значит взять весь столбец под индексом 1

16. получится:

```text
name salary
B    100
C    200
A    300
```

17. обратный порядок

```text
name salary
A    300
C    200
B    100
```

18. нет. Потому что он работает как питоновский `sorted()`, а не как `.sort()`

19. в случае `df.sort_values("salary")` будет DataFrame, отсортированный по salary, а во втором случае сначала возьмем Series salary, а потом отсортируем только его (только этот столбец)

20. **Moscow: 3, Kazan: 2, Berlin: 1**

21.

```python
df["city"].value_counts().idxmax()
```

22.

```python
df["city"].value_counts().max()
```

23. он сгруппирует кучки, у которых одинаковые "user". То есть будут группки "A": [100, 300] и "B": [200, 500, 100]. то есть объединяет строки в группы по определенному признаку

24. найдет среднее на каждой группе. У "A" будет 200, у "B" будет 800/3

25. посчитает сумму у каждого юзера. "A" = 300, "B" = 800

26. найдет максимальное в группе. "A" = 200, "B" = 500

27. среднее:

```python
df.groupby("user")["price"].agg(["mean", "sum", "min", "max"])
```

28. count считает количество не-NaN значений для каждой группы по столбцу price (группируем по user)

29. `"age": 1, "salary": 1`

30. `(20+30+40)/3 = 30`

31. потому что mean() не будет учитывать NaN

32. заполнит все NaN нулями в столбце "age"

33. нет, он возвращает новый, а не меняет старый

34.

```python
df = df["age"].fillna(0)
```

35. удаляет все строки, где есть NaN в хотя бы одном столбце

36. две. 20 100 и 40 400

37. останутся id 1,2 (тк для 3 и 4 нет пары в другом датафрейме)

38. 1,2,3, так как мы сохраняем левый датафрейм

39. 1,2,3,4, так как мы объединяем оба датафрейма, а там где не хватает пары запишем NaN.

40. получит NaN в price

41. значит оставляем те ключи, которые есть в обеих таблицах одновременно

42. сохраняем ключи из левой, если есть пересечения с правой, присоединяем их

43. будет 2\*2 = 4

44. потому что для каждого айди из левой найдется каждый айди из правой

45. 3\*4 = 12

46. когда две таблицы делают merge, причем слева и справа есть дубликаты одного ключа (то что мы до этого писали)

47. concat() объединяет (склеивает) таблицы (если axis=0, то названия столбцов должны совпадать. если axis=1, то совместим по индексам), а merge объединяет по некотормоу sql правилу (по умолчанию inner)

48. `concat`

49. `merge`

50. потому что из-за concat мы посчитаем среднее на всем датасете, а не на трейн. получится утечка данных

51.

```python
mean = train["age"].mean()
train["age"] = train["age"].fillna(mean)
test["age"] = test["age"].fillna(mean)
```

52. b, d (d не утечка, но мы не заполнили train... с другой стороны, может, там не было пропущенных значений?)

53.

```text
name age
B  30
D  25
```

54. все будет работать, mean не учитывает NaN. просто посчитает среднее без него

55.

```python
df["city"].value_counts()["Kazan"]
```

56. посчитает среднее на значениях столбца salary, где age >= 18 (loc возьмет те индексы строк, которые выдаст `df["age"] >= 18`, а потом возьмет из этих строк только столбец salary)

57. нужно проверить насколько сильно раздуетсчя таблица, так как у нас будет умножение количества одинаковых user_id слева и справа

58. many-to-many, потому что таблицу раздувается перемножением количества строк по одинаковым ключам

59.

```python
df.groupby("user_id")["price"].count()
```

60. нужна будет сортировка по дате заказа. что-то вроде

```python
df.sort_values("date", ascending=False)
```

после чего уже делать groupby (он сохраняет порядок?)

61. count не учтет Nan, поэтому 3

62. не учитывает NaN, так что 30

63. в первом случае вернетсч dataframe со строками, где age больше 30, во втором случае будет series, который вернет только те строки возраста, где возраст больше 30.

64. в первом случае series, во втором dataframe

65. строки с этим ключом в итоговую таблицу включены не будут, так что количество строк уменьшится

66. количество строк не изменится, мы сохраняем ключи левой таблицы

67. нет, потому что тогда train получит данные из test, это утечка данных, которые модель не должна знать на момент обучения

68. не всегда. Число строк не уменьшится, если каждый ключ встречается ровно 1 раз

69. не всегда. inner может умнеьшить количество строк

70. правда, можно написать

```python
df["value"],value_counts()
```
